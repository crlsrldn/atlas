node_modules/
dist/
build/
.env
.env.local
preferences.json
.svelte-kit/
target/
.DS_Store

# Agent files
.gemini
GEMINI.md
PRD.md
IMPLEMENTATION_PLAN.md

core/playback_history.json
core/preferences.json

# Gateway build output — CI builds Linux binaries in Docker, so local ones
# (and stray pid files) must never be committed.
gateway/atlas-gateway
gateway/gateway-bin
gateway/gateway_bin
gateway/gateway_test
gateway/*.pid

# macOS AppleDouble metadata (created on exFAT/network volumes)
._*
**/._*
